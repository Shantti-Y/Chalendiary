const listSubheader = {
  lineHeight: '20px'
}

const currentTag = {
  margin: '8px 0 12px 0'
}

const listItem = {
  display: 'flex'
}

const typography = {
  fontSize: '14px',
  fontWeight: 500,
  overflow: 'hidden',
  whiteSpace: 'nowrap',
  textOverflow: 'ellipsis'
}

const icon = {
  fontSize: '18px'
}

const button = {
  minWidth: '18px',
  padding: '3px',
  borderRadius: '10px',
  float: 'right'
}

export default { listSubheader, currentTag, listItem, typography, icon, button }