const menuBarWidth = 256;

const drawerForMobile = {
  width: menuBarWidth,
  flexShrink: 0
}

const drawerForDesktop = {
  width: menuBarWidth
}

const drawerPaper = {
  width: menuBarWidth
};

const list = {
  padding: 0
};

export default { list, drawerForMobile, drawerForDesktop, drawerPaper };